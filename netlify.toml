[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_PATHS = "docs/**,*.md,.env.example,DEPLOYMENT_READY.md,CONFIG_SETUP.md,README.md,WORKSPACE_CONTEXT.md"
